import { Router } from 'express';

const router = Router();

// Placeholder for habit routes
// router.get('/', getHabitsController);
// router.post('/', createHabitController);

export default router;
